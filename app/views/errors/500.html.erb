<%= "Sorry, but the page couldn't be displayed because of server-side error."%><br />
<% if @show_backtrace %>
    <br />
    Stacktrace:<br />
    <textarea rows="30" cols="78"><%= @exception.to_s %>
<%= @exception.backtrace %></textarea>
<% end %>

<br />